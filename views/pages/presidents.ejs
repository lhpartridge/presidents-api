<%-include ('../partials/header'); %>

<main class="main main-presidents container">
    <h2 class="main-h2"><%= name %></h2>
    <!-- carousel adapted from Bootstrap v5.1 -->
    <!-- <section class="carousel-section">
        <div id="carouselExampleCaptions" class="carousel slide" data-bs-ride="carousel">
            <div class="carousel-indicators">
              <button type="button" data-bs-target="#carouselExampleCaptions" data-bs-slide-to="0" class="active" aria-current="true" aria-label="Slide 1"></button>
              <button type="button" data-bs-target="#carouselExampleCaptions" data-bs-slide-to="1" aria-label="Slide 2"></button>
              <button type="button" data-bs-target="#carouselExampleCaptions" data-bs-slide-to="2" aria-label="Slide 3"></button>
            </div>
            <div class="carousel-inner">
              <div class="carousel-item active">
                <img src="..." class="d-block w-100" alt="...">
                <div class="carousel-caption d-none d-md-block">
                  <h5>First slide label</h5>
                  <p>Some representative placeholder content for the first slide.</p>
                </div>
              </div>


                <% data.forEach(item => { %>
                    <div class="carousel-item">
                        <a href="/presidents/<%= item.id %>">
                            <img src="<%= item.photo %>" alt="<%= item.title %>" class="img-fluid d-block w-40">

                        </a>

                        <div class="carousel-caption d-none d-md-block">
                          <h5><%= item.name %></h5>
                          <p>President:  <%= item.ordinal %></p> <p></p>Term:  <%= item.yearsInOffice %></p>
                        </div>

         
                <% }) %>
              </div>
            </div>
            <button class="carousel-control-prev" type="button" data-bs-target="#carouselExampleCaptions" data-bs-slide="prev">
              <span class="carousel-control-prev-icon" aria-hidden="true"></span>
              <span class="visually-hidden">Previous</span>
            </button>
            <button class="carousel-control-next" type="button" data-bs-target="#carouselExampleCaptions" data-bs-slide="next">
              <span class="carousel-control-next-icon" aria-hidden="true"></span>
              <span class="visually-hidden">Next</span>
            </button>
          </div>
    </section> -->
    <section class="section section-president">
        <div class="row justify-content-around">
                    <!-- title, photo, term -->
        <% data.forEach(item => { %>
            <div class="col-md-4">
                <figure class="figure figure-president">
                    <a href="/presidents/<%= item.id %>">
                        <img src="<%= item.photo %>" alt="<%= item.title %>" class="img-fluid president-image">
                        <figcaption class="figcaption figcap-president text-center"><%= item.name %></figcaption>
                    </a>
                    <figcaption class="figcaption figcap-term text-center">President:  <%= item.ordinal %></figcaption>
                    <figcaption class="figcaption figcap-term text-center"><%= item.yearsInOffice %></figcaption>
                </figure>                  
            </div>
 
        <% }) %>
        </div>
    </section>
</main>
 
<%-include ('../partials/footer'); %>